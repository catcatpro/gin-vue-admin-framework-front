import { describe, it, expect } from 'vitest'
import toolbox from '../toolbox'

describe('getCurrentRouter', () => {
    it('Get Current Router', () => {
        // const res = toolbox.getCurrentRouterPath()
        // if (res)
        //     expect(res.toString()).toContain('/database_management')
    })
})